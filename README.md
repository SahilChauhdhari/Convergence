# Nexus Comms

A high-fidelity terminal-based communication interface with role-based access control.

## Setup
1. Open `src/index.html` in any modern web browser.
2. Use the terminal interface to execute commands.

## Core Commands
- `/help` - List available commands.
- `/login [username]` - Access the system.
- `/join [room]` - Switch communication channels.
